# Method

Expand all internal theorem dependencies, pin external theorem imports, recompute exact identities and limits, require zero unresolved nodes, then run an independent checker and one invalid mutation per claim.
