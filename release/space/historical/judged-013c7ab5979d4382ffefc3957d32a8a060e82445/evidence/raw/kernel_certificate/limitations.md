# Scope

The kernel checks the reproduced implication chain and exact algebra. Named analytic primitives (the paper's weighted L1-L2 theorem and the pinned Jia local-entropy result) remain explicit theorem dependencies; they are never replaced by numerical samples or hidden assumptions.
